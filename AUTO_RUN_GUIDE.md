# 每日自动运行设置指南

## 简介

本指南教你如何让电脑每天自动运行 `auto_run.py`，无需手动操作。

## 一、Windows 任务计划程序设置

### 步骤 1：打开任务计划程序

1. 按 `Win + R` 打开运行窗口
2. 输入 `taskschd.msc`
3. 按回车键打开任务计划程序

### 步骤 2：创建基本任务

1. 点击右侧 **"创建基本任务"**
2. 名称填写：`每日市场分析`
3. 点击 **下一步**

### 步骤 3：设置触发器

1. 选择 **"每天"**
2. 点击 **下一步**
3. 设置时间：`15:10`（下午3点10分）
4. 点击 **下一步**

### 步骤 4：设置操作

1. 选择 **"启动程序"**
2. 点击 **下一步**
3. 在"程序或脚本"栏填入：
   ```
   python
   ```
4. 在"添加参数"栏填入（注意引号内填你保存的路径）：
   ```
   "C:\Users\你的用户名\mvp-stock-system\auto_run.py"
   ```
5. 点击 **下一步**

### 步骤 5：完成

1. 勾选 **"打开此任务的高级属性"**
2. 点击 **完成**

### 步骤 6：检查设置（重要！）

1. 找到刚创建的任务，右键点击 → **属性**
2. 在"常规"选项卡中：
   - 勾选 **"无论用户是否登录都要运行"**
   - 勾选 **"使用最高权限运行"**
3. 点击 **确定**

## 二、运行时间建议

**推荐时间：15:10**

原因：
- A股收盘时间是15:00
- 15:10 可以确保获取到当天的完整数据
- 如果遇到节假日，数据会自动跳过

## 三、常见问题

### Q1：任务没有运行怎么办？

1. 检查Python是否添加到系统PATH
2. 尝试使用完整Python路径，如：
   ```
   "C:\Users\你的用户名\AppData\Local\Programs\Python\Python310\python.exe"
   "你的文件夹路径\auto_run.py"
   ```

### Q2：报告没有生成？

1. 检查 `reports` 文件夹是否存在
2. 检查 `data/raw` 和 `data/processed` 文件夹是否有权限

### Q3：如何手动测试？

打开命令行，进入项目文件夹，运行：
```
python auto_run.py
```

如果显示"今日市场分析完成"即为正常。

### Q4：如何修改运行时间？

1. 打开任务计划程序
2. 找到"每日市场分析"任务
3. 右键 → 属性 → 触发器 → 编辑
4. 修改时间后保存

### Q5：如何停止自动运行？

1. 打开任务计划程序
2. 找到"每日市场分析"任务
3. 右键 → 删除

## 四、检查运行结果

每天运行后，可以在 `reports` 文件夹查看报告：
- 文件名格式：`2026-05-08.md`
- 用记事本或编辑器打开即可查看

---

**提示**：如果你的用户名不是中文，请将上面路径中的"周萱"改成你的实际用户名。
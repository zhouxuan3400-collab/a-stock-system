# 部署到 Streamlit Cloud

## 正确填写方式

请按以下信息填写部署表单：

- **Repository**: `zhouxuan3400-collab/a-stock-system`
- **Branch**: `main`（不是 master）
- **Main file path**: `app.py`（不是 streamlit_app.py）

## 一键部署链接

直接点击下方链接会自动填充正确信息：

**https://share.streamlit.io/new?repository=zhouxuan3400-collab/a-stock-system&branch=main&file=app.py**

## 常见问题

1. **Branch 错误**: 填 `main`（不是 master）
2. **文件不存在**: 填 `app.py`（项目根目录已有）
3. **仓库名**: 填 `zhouxuan3400-collab/a-stock-system`（不是 repo）

填好后点击 Deploy 即可。